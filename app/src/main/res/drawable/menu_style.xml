<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Color de fondo del borde -->
    <stroke
        android:width="1dp"
        android:color="@color/lightBrown" />
    <corners android:radius="0dp" /> <!-- Radio de las esquinas (0dp para bordes rectos) -->
</shape>